require "spec_helper"

RSpec.describe BNTop20 do
  it "has a version number" do
    expect(BNTop20::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
